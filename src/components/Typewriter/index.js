import React, { Component } from 'react';

class TypeWriter extends Component {
  render() {
    return (
      <span>
        {this.props.title}
      </span>
    );
  }
}

export default TypeWriter;
